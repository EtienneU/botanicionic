import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'botanicionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
